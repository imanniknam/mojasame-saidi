# گروه مسیر فروشگاه `(store)`

صفحات آینده (مثلاً `/`, `/categories`, `/products/[slug]`, `/cart`, ...) در این گروه قرار می‌گیرند تا:

- `layout` اختصاصی فروشگاه (هدر، ناوبری پایین موبایل، فوتر)
- جداسازی از پنل ادمین و APIهای عمومی

فعلاً **هیچ صفحه‌ی فروشگاهی** در این مخزن پیاده‌سازی نشده است.
