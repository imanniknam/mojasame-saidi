# Copy to `.env` locally (filename blocked in some environments as `.env.example`)

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/mojasame_saidi?schema=public"
NEXT_PUBLIC_SITE_URL="https://mojasamesaidi.ir"

# Auth placeholder — configure in implementation phase (e.g. NextAuth)
AUTH_SECRET="replace-with-long-random-string"
